<nav class="bg-zinc-800 shadow w-full fixed top-0 px-8 md:px-auto mb-10 py-3 z-50">
            <div class="md:h-16 h-28 mx-auto md:px-4 container flex items-center justify-between flex-wrap md:flex-nowrap">
                <div class="text-emerald-500 md:order-1">
                </div>
                <div class="text-zinc-50 order-3 w-full md:w-auto md:order-2">
                    <ul class="flex font-semibold justify-between">
                        <!-- Active Link = text-emerald-500
                        Inactive Link = hover:text-emerald-500 -->
                        <li class="md:px-4 md:py-2 hover:text-emerald-400"><a href="{{ path('app_product_index') }}">Produits</a></li>
                        <li class="md:px-4 md:py-2 hover:text-emerald-400"><a href="{{ path('app_category_index') }}">Catégories</a></li>
                        {% if app.user %}
                            <li class="md:px-4 md:py-2 hover:text-emerald-400"><a href="{{ path('app_location_index') }}">Emplacements</a></li>
                            <li class="md:px-4 md:py-2 hover:text-emerald-400"><a href="{{ path('app_history_index') }}">Prêts/Historiques</a></li>
                            <li class="md:px-4 md:py-2 hover:text-emerald-400"><a href="{{ path('app_repair_index') }}">Réparations</a></li>
                            <li class="md:px-4 md:py-2 hover:text-emerald-400"><a href="{{ path('app_dashboard') }}">Dashboard</a></li>
                        {% endif %}
                        {% if app.user and is_granted('ROLE_ADMIN') %}
                            <li class="md:px-4 md:py-2 hover:text-emerald-400"><a href="{{ path('app_user') }}">Utilisateurs</a></li>
                        {% endif %}
                    </ul>
                </div>
                <div class="order-2 md:order-3">
                    {% if app.user %}
                        <a href="{{ path('app_logout') }}">
                            <button class="px-4 py-2 bg-emerald-500 hover:bg-emerald-600 text-gray-50 rounded-xl flex items-center gap-2">
                                <!-- Heroicons - Login Solid -->
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M3 3a1 1 0 011 1v12a1 1 0 11-2 0V4a1 1 0 011-1zm7.707 3.293a1 1 0 010 1.414L9.414 9H17a1 1 0 110 2H9.414l1.293 1.293a1 1 0 01-1.414 1.414l-3-3a1 1 0 010-1.414l3-3a1 1 0 011.414 0z" clip-rule="evenodd" />
                                </svg>
                                <span>Déconnexion</span>
                            </button>
                        </a>
                    {% else %}
                        <a href="{{ path('app_login') }}">
                            <button class="px-4 py-2 bg-emerald-500 hover:bg-emerald-600 text-gray-50 rounded-xl flex items-center gap-2">
                                <!-- Heroicons - Login Solid -->
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M3 3a1 1 0 011 1v12a1 1 0 11-2 0V4a1 1 0 011-1zm7.707 3.293a1 1 0 010 1.414L9.414 9H17a1 1 0 110 2H9.414l1.293 1.293a1 1 0 01-1.414 1.414l-3-3a1 1 0 010-1.414l3-3a1 1 0 011.414 0z" clip-rule="evenodd" />
                                </svg>
                                <span>Connexion</span>
                            </button>
                        </a>
                    {% endif %}
                </div>
            </div>
        </nav>