{% extends 'base.html.twig' %}

{% block title %}New Condition{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/form.css') }}">
{% endblock %}

{% block body %}
    <h1>Create new Condition</h1>

    {{ include('condition/_form.html.twig') }}

    <a class="back-btn" href="{{ path('app_condition_index') }}">back to list</a>
{% endblock %}
